---
# Allowed version bumps: patch, minor, major
jahia-oauth: patch
---

Fixed the OAuth connector buttons so a title, CSS class or HTML id that contains special characters now renders as written. A sign-in button now accepts a result only from the popup it opened, and reports the outcome as a strict true or false. Opening the result page directly no longer stops its script.
