---
jahia-oauth: patch
---

Fixed the OAuth connector buttons so a title, CSS class or HTML id that contains special characters now renders as written. Fixed the OAuth popup so it hands its result only to the page that opened it. The popup now reports the sign-in outcome as a strict true or false.
